[Unit]
Description=HSVE FastAPI Web
After=network.target

[Service]
WorkingDirectory=%h/jesus-cartel-production
Environment=PYTHONUNBUFFERED=1
ExecStart=/bin/bash -lc 'source ~/miniconda3/etc/profile.d/conda.sh && conda run -n audio uvicorn web.app:app --host 0.0.0.0 --port 8000'
Restart=always

[Install]
WantedBy=default.target

